1。当表头发生改变时。应该先销毁datatables的实力对象
   然后清空表格的tbody内容后再创建新的实力对象。不然会报错（因为再一瞬间表头跟tabody列数不一致）；
   也可以销毁对象后重新绘制tables（静态的纯html）然后创建datatables的实例；

   总结：当表头发生改变是。先销毁实例。然后清空表头和表体。绘制表头-创建实例


   步骤：1销毁现有实例

        2清空表头表体

        3绘制表头

        4创建新的实例